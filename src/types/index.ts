export interface ICampaignEntry {
  bidAmount: number;
  campaignFund: number;
  campaignName: string;
  keywords: string[];
  radius: number;
  status: boolean;
  town: string;
  id: string;
}

export interface IBasicEntry {
  id: string;
  name: string;
}
